# NodeOS boot singleUser

Load and prepare a minimal system to exec a diskless single process / single
user session on NodeOS. The per-user root filesystem will need to be located on
`/tmp` on the initram tarfile.
